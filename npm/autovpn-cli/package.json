{
  "name": "@swimmingliu/autovpn",
  "version": "1.3.0",
  "description": "npm wrapper for the AutoVPN headless CLI",
  "type": "module",
  "bin": {
    "autovpn": "bin/autovpn.mjs"
  },
  "files": [
    "bin/",
    "dist/",
    "lib/",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "npm run build",
    "test": "npm run build && node --test test/*.test.mjs test/parity/*.test.mjs test/jobs/*.test.mjs test/pipeline/*.test.mjs"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5"
  }
}
