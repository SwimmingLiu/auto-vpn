# VPN Subscription Automation 限制的 TODO（2026-04-29）

## 说明

本文档只用于归档当前项目里**已识别但不进入本轮实施**的受限事项。

这些事项的共同特点是：

- 不属于当前 `sub-nodes` 部署对齐的必要交付
- 会引入额外的平台识别、兼容性、可维护性或审计风险
- 当前不作为主线任务直接实现

处理原则：

- 只记录，不在本轮开发中落地
- 如后续确需推进，必须先补充单独设计、验证方案和回滚方案
- 未完成前，不应写入当前主线“已完成”或“待发布”范围

---

## 限制的 TODO

### R1：Worker 产物特征处理（限制项）

- [ ] [限制] 评估是否需要处理 `_worker.js` 中与协议或业务用途强绑定的显著文本特征，并先明确：
  - 处理目标是什么
  - 是否会影响功能正确性
  - 是否会影响线上校验与排障

- [ ] [限制] 如果后续需要做文本特征处理，先补一份实验清单：
  - 原始产物样本
  - 变换后产物样本
  - 行为一致性校验方法
  - 失败回滚步骤

### R2：构建期随机化（限制项）

- [ ] [限制] 评估是否需要在 `_worker.js` 构建过程中加入可控随机化，并先明确：
  - 是否真的解决当前问题
  - 是否破坏可重复构建
  - 是否增加 diff、回归和发布排查成本

- [ ] [限制] 如要继续推进，先定义最小验证集：
  - 同一输入多次构建的差异范围
  - 与功能无关的允许变动边界
  - 构建失败或线上异常时的快速定位方式

### R3：固定模板特征评估（限制项）

- [ ] [限制] 评估当前 Worker 模板固定结构是否真的是问题来源，避免在没有证据前扩大改动范围

- [ ] [限制] 如需继续，先准备一份证据清单：
  - 当前模板版本
  - 线上产物样本
  - 触发问题的具体现象
  - 与模板结构相关的可复现实验

### R4：代码结构变换与可维护性（限制项）

- [ ] [限制] 评估更激进的代码结构变换是否会影响：
  - 可读性
  - 可调试性
  - 回滚速度
  - 与现有 render / obfuscate / deploy / verify 链路的兼容性

- [ ] [限制] 如果后续继续推进，先单独补文档说明：
  - 变换前后结构差异
  - 对构建链路的影响
  - 对测试与验收方式的影响
  - 最小回滚路径

---

## 暂不纳入本轮验收

以下内容**不属于**本轮 `sub-nodes` 对齐任务的验收条件：

- Worker 产物额外特征处理
- 构建期随机化
- 模板级结构扰动
- 更强的代码结构变换或额外保护层

本轮验收仍以以下事项为准：

- `sub-nodes` 正式部署链路可用
- 设置页 deploy 配置可编辑且可持久化
- `_worker.js` 命名与 Pages 目录部署链路一致
- 旧配置迁移正常
- 当前测试通过
